server:
  port: 4000
spring:
  application:
    name: raise-crowd-auth
  thymeleaf:
    prefix: classpath:/thymeleaf/
    suffix: .html
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1000/eureka